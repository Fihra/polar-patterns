import React from "react";

const About = () => {
    return (
        <div className="about-container">
            <p>A <b>polar pattern</b> is a representation for a microphone's area of the audio signal input with the sound pressure. This displays the direction(s) of a mic in which it picks up sound and the direction(s) a mic will block sound.</p>
        </div>
    )
}

export default About;