import React from "react";

const About = () => {
    return (
        <div className="about-container">
            <p>A <b>polar pattern</b> is a representation of a microphone's directional sensitivity to sound pressure. In other words, polar patterns tell which direction(s) a mic will be sensitive to picking up sound and which direction(s) a mic will reject sound.</p>
        </div>
    )
}

export default About;